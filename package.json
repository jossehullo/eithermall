{
  "name": "eithermall",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && node server.js",
    "client": "cd client && npm run dev",
    "dev": "npm-run-all --parallel server client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2"
  }
}
